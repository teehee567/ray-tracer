{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "raytracer.exe",
            "program": "${workspaceFolder}/target/release/raytracer.exe",
            "args": [],
            "preLaunchTask": "rust: cargo build",
            "cwd": "${workspaceFolder}"
        }
    ]
}
